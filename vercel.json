{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_BLACKBOX_CUSTOMER_ID": "@blackbox_customer_id",
    "NEXT_PUBLIC_BLACKBOX_API_KEY": "@blackbox_api_key"
  }
}
